// Stub module for @mediapipe/pose
// MoveNet uses the TensorFlow.js backend, not the MediaPipe backend.
// This stub prevents Turbopack from failing on the unused mediapipe import
// that exists inside @tensorflow-models/pose-detection.
export const Pose = {};
export default {};
